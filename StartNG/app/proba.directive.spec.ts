import { ProbaDirective } from './proba.directive';

describe('ProbaDirective', () => {
  it('should create an instance', () => {
    const directive = new ProbaDirective();
    expect(directive).toBeTruthy();
  });
});
